#/bin/sh 

#add note entries to the file
#$HOME/note-file

file=$HOME/note-file
echo -n "Enter Title: "
date >> $file
read title
echo "$title" >> $file
echo >> $file
cat >> $file
echo "----------------------------------" >> $file
echo >> $file
